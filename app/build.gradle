apply plugin: 'com.android.application'
apply plugin: 'kotlin-android'
//apply plugin: 'com.didiglobal.booster'
//apply plugin: "com.siy.tansaga.local"
apply plugin: "com.siy.tansaga"

android {
    compileSdk 32

    defaultConfig {
        applicationId "com.siy.tansaga"
        minSdk 21
        targetSdk 32
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = '1.8'
    }
}

dependencies {
    implementation fileTree(dir: "libs", include: ["*.jar", "*.aar"])
    implementation 'androidx.core:core-ktx:1.7.0'
    implementation 'androidx.appcompat:appcompat:1.4.1'
    implementation 'com.google.android.material:material:1.6.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.0.4'
    implementation 'androidx.navigation:navigation-fragment-ktx:2.3.5'
    implementation 'androidx.navigation:navigation-ui-ktx:2.3.5'


  //  implementation "com.github.siywu:tansaga:1b1ce07413"
 //   implementation project(':tansaga-base')
}


TExtension {
    proxys {
        //代理用户方法
        proxy {
            targetClass = "com.siy.tansaga.test.OriginJava"
            hookMethod = "hookProxy"
            hookClass = "com.siy.tansaga.test.HookJava"
            filter "com.siy.tansaga.*"
        }

        //代理系统方法
        getString {
            targetClass = "android.content.Context"
            hookMethod = "hookProxySys"
            hookClass = "com.siy.tansaga.test.HookJava"
            filter "com.siy.tansaga.test.*", "com.siy.tansaga.MainActivity"
        }

    }

    //修改用户方法
    replaces {
        replace {
            targetClass = "com.siy.tansaga.test.OriginJava"
            hookMethod = "hookReplace"
            hookClass = "com.siy.tansaga.test.HookJava"
        }
    }
}
